{
  "name": "@test-org/aliases",
  "version": "0.0.1",
  "description": "Internal, shared utilities",
  "author": "Yuri Zelenskiy",
  "scripts": {
    "_start": "tsdx watch --tsconfig tsconfig.json --verbose --noClean --format cjs --target node",
    "build": "tsdx build --tsconfig tsconfig.json --target node",
    "test": "tsdx test",
    "lint": "eslint ./src --ext ts --ext tsx",
    "prepublish": "npm run build"
  },
  "main": "dist/index.js",
  "module": "dist/form1.esm.js",
  "typings": "dist/index.d.ts",
  "files": [
    "README.md",
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT"
}
